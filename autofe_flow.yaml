---
title: Auto Feature Engineering #rameter
version: 1.0.0 # Required
description: Automatic feature engineering
long_description: This blueprint is to using Intel e2eAIOK RecDP toolkit to automatically convert customer data to ready-to-train feature enriched data.
# Optional properties for crediting authors
author: "Intel"
author_email: "info@cnvrg.io"

tags:
  - training

# List of tasks (libraries)
tasks:
  - title: Fetch Data
    top: 126
    left: 9

    # Type must be according to the flow task conventions (data, exec, deploy)
    type: exec

    # The library data
    library: autofe-fetch-data
    library_version: 1.0.0

    requirements:
      cpu: 24
      memory: 48
      gpu: 0
      hpu: 0

    image: aiops/autofe:pa
    language: python3

    params:
      - key: url
        type: 'categorical'
        values:
        - sample.csv
      - key: usage
        type: 'categorical'
        values:
        - 'combined'

  - title: Data Split
    top: 126
    left: 217

    # Type must be according to the flow task conventions (data, exec, deploy)
    type: exec

    # The library data
    library: autofe-data-split
    library_version: 1.0.0

    # The flattened library parameters (in this case we have training library)
    command: python3 dataset_split.py

    requirements:
      cpu: 24
      memory: 48
      gpu: 0
      hpu: 0

    image: aiops/autofe:pa
    language: python3

    # The "arguments" property changes to "params", rest remains the same.
    # Params in the blueprint yaml can be used to override the arguments in the library.
    params:
      - key: workspace
        type: 'categorical'
        values:
          - 'output'
      - key: input_file
        type: 'categorical'
        values:
          - '/input/fetch_data/output/dataset.parquet'

  - title: Gen Pipeline
    top: 126
    left: 458

    # Type must be according to the flow task conventions (data, exec, deploy)
    type: exec

    # The library data
    library: autofe-gen-pipeline
    library_version: 1.0.0

    # The flattened library parameters (in this case we have training library)
    command: python3 gen_pipeline.py

    requirements:
      cpu: 24
      memory: 48
      gpu: 0
      hpu: 0

    image: aiops/autofe:pa
    language: python3

    # The "arguments" property changes to "params", rest remains the same.
    # Params in the blueprint yaml can be used to override the arguments in the library.
    params:
      - key: workspace
        type: 'categorical'
        values:
          - 'output'
      - key: target_label
        type: 'categorical'
        values:
          - 'fare_amount'
      - key: input_file
        type: 'categorical'
        values:
          - '/input/train_test_split/output/train_sample.parquet'

  - title: Fit Transform Train
    top: 28
    left: 726

    # Type must be according to the flow task conventions (data, exec, deploy)
    type: exec

    # The library data
    library: autofe-transform
    library_version: 1.0.0
    # The flattened library parameters (in this case we have training library)
    command: python3 data_transform.py

    requirements:
      cpu: 24
      memory: 48
      gpu: 0
      hpu: 0

    image: aiops/autofe:pa
    language: python3

    # The "arguments" property changes to "params", rest remains the same.
    # Params in the blueprint yaml can be used to override the arguments in the library.
    params:
      - key: workspace
        type: 'categorical'
        values:
          - 'output'
      - key: target_label
        type: 'categorical'
        values:
          - 'fare_amount'
      - key: engine_type
        type: 'categorical'
        values:
          - 'pandas'
      - key: train
        type: 'categorical'
        values:
          - 'True'
      - key: input_file
        type: 'categorical'
        values:
          - '/input/train_test_split/output/train_sample.parquet'
      - key: pipeline_output
        type: 'categorical'
        values:
          - '/input/gen_pipeline/output'
                    
  - title: Transform Test
    top: 226
    left: 724

    # Type must be according to the flow task conventions (data, exec, deploy)
    type: exec

    # The library data
    library: autofe-transform
    library_version: 1.0.0

    # The flattened library parameters (in this case we have training library)
    command: python data_transform.py

    requirements:
      cpu: 24
      memory: 48
      gpu: 0
      hpu: 0

    image: aiops/autofe:pa
    language: python3

    # The "arguments" property changes to "params", rest remains the same.
    # Params in the blueprint yaml can be used to override the arguments in the library.
    params:
      - key: workspace
        type: 'categorical'
        values:
          - 'output'
      - key: target_label
        type: 'categorical'
        values:
          - 'fare_amount'
      - key: engine_type
        type: 'categorical'
        values:
          - 'pandas'
      - key: train
        type: 'categorical'
        values:
          - 'False'
      - key: input_file
        type: 'categorical'
        values:
          - '/input/train_test_split/output/test_sample.parquet'
      - key: pipeline_output
        type: 'categorical'
        values:
          - '/input/gen_pipeline/output'

  - title: Train Model
    top: 28
    left: 981

    # Type must be according to the flow task conventions (data, exec, deploy)
    type: exec

    # The library data
    library: autofe-train
    library_version: 1.0.0

    # The flattened library parameters (in this case we have training library)
    command: python lgbm_train.py

    requirements:
      cpu: 24
      memory: 48
      gpu: 0
      hpu: 0

    image: aiops/autofe:pa
    language: python3

    # The "arguments" property changes to "params", rest remains the same.
    # Params in the blueprint yaml can be used to override the arguments in the library.
    params:
      - key: workspace
        type: 'categorical'
        values:
          - 'output'
      - key: target_label
        type: 'categorical'
        values:
          - 'fare_amount'
      - key: train
        type: 'categorical'
        values:
          - 'True'
      - key: input_file
        type: 'categorical'
        values:
          - '/input/fit_transform_train/output/transformed_train_data.parquet'
      - key: pipeline_output
        type: 'categorical'
        values:
          - '/input/gen_pipeline/output'

  - title: Test
    top: 227
    left: 1226

    # Type must be according to the flow task conventions (data, exec, deploy)
    type: exec

    # The library data
    library: autofe-train
    library_version: 1.0.0

    # The flattened library parameters (in this case we have training library)
    command: python lgbm_train.py

    requirements:
      cpu: 24
      memory: 48
      gpu: 0
      hpu: 0

    image: aiops/autofe:pa
    language: python3

    # The "arguments" property changes to "params", rest remains the same.
    # Params in the blueprint yaml can be used to override the arguments in the library.
    params:
      - key: workspace
        type: 'categorical'
        values:
          - 'output'
      - key: target_label
        type: 'categorical'
        values:
          - 'fare_amount'
      - key: train
        type: 'categorical'
        values:
          - 'False'
      - key: input_file
        type: 'categorical'
        values:
          - '/input/transform_test/output/transformed_test_data.parquet'
      - key: pipeline_output
        type: 'categorical'
        values:
          - '/input/gen_pipeline/output'

relations:
- from: Fetch Data
  to: Data Split
- from: Data Split
  to: Gen Pipeline
- from: Gen Pipeline
  to: Fit Transform Train
- from: Fit Transform Train
  to: Transform Test
- from: Fit Transform Train
  to: Train Model
- from: Train Model
  to: Test
- from: Transform Test
  to: Test